# Username Validator

A small Python learning project for implementing and testing reusable username validation.

## Setup

```bash
python -m pip install -e ".[test]"
```

## Run tests

```bash
pytest
```

The baseline validator rejects empty, short, and non-string usernames. Further improvements are tracked through GitHub issues.
